// 1. Declare três variáveis com tipos explícitos
let nome: string = "João";
let idade: number = 20;
let ativo: boolean = true;

// Exiba no console uma frase
console.log(`Usuário ${nome}, idade ${idade}, está ativo? ${ativo}`);