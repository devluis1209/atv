// 3. Crie uma função somar
function somar(a: number, b: number): number {
    return a + b;
}

// Exiba no console o resultado usando template string
const resultado: number = somar(5, 3);
console.log(`A soma é ${resultado}`);